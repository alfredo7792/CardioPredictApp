// import axios from 'axios';

// const API_URL = 'https://your-api-url.com';

// export const getRevisions = async () => {
//   const response = await axios.get(`${API_URL}/revisions`);
//   return response.data;
// };

// export const getRevisionById = async (id: number) => {
//   const response = await axios.get(`${API_URL}/revisions/${id}`);
//   return response.data;
// };

// export const addRevision = async (revision: any) => {
//   const response = await axios.post(`${API_URL}/revisions`, revision);
//   return response.data;
// };

// export const updateRevision = async (id: number, revision: any) => {
//   const response = await axios.put(`${API_URL}/revisions/${id}`, revision);
//   return response.data;
// };

// export const deleteRevision = async (id: number) => {
//   await axios.delete(`${API_URL}/revisions/${id}`);
// };
